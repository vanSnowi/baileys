node_modules/
*.log
.DS_Store
.env
.npmrc
*.tgz

# WhatsApp APK dex dumps (analysis inputs, keep local only)
extracts/*.dex
extracts/*.db
package-lock.json

# WhatsApp Web source dump (analysis input, local only)
extracts/wa-web/
